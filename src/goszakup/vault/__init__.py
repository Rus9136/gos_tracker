"""KeyVault — шифрованное хранилище секретов клиента для автоподачи заявок.

Храним чужие ЭЦП-ключи (.p12), пароли учёток портала и PIN — высочайший
уровень ответственности (см. TENDER_ECP_SIGNING_GUIDE.md §8). На диске/в БД
лежит ТОЛЬКО шифртекст (AES-256-GCM) + nonce; расшифровка — в RAM и только в
момент использования. Мастер-ключ — из окружения (`GZ_VAULT_MASTER_KEY`),
в проде должен приходить из KMS/HSM, не лежать рядом с кодом.
"""
